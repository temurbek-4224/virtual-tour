// Root layout — minimal shell; locale layout provides html/body
import type { ReactNode } from 'react';

export default function RootLayout({ children }: { children: ReactNode }) {
  return children;
}
